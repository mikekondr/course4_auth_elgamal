<Window x:Class="auth_elgamal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:auth_elgamal.ViewModels"
        xmlns:local="clr-namespace:auth_elgamal"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="MainWindow" Height="650" Width="850"
        Icon="/favicon.ico"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <materialDesign:DialogHost Identifier="RootDialogHost">
        <Grid>
            <ContentControl Content="{Binding CurrentViewModel}" />

            <materialDesign:Snackbar
                MessageQueue="{Binding NotificationQueue}"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Right"
                Margin="15"
    
                Background="Transparent"
                Padding="0"
                BorderThickness="0">

                <materialDesign:Snackbar.Resources>
                    <DataTemplate DataType="{x:Type sys:String}">
                        <Border Background="{DynamicResource MaterialDesignSnackbarBackground}"
                                CornerRadius="4" MinWidth="288" MaxWidth="568">
                            <TextBlock Text="{Binding}"
                                       Margin="16,10"
                                       Foreground="{DynamicResource MaterialDesignSnackbarForeground}"
                                       TextWrapping="Wrap"/>
                        </Border>
                    </DataTemplate>
                </materialDesign:Snackbar.Resources>

            </materialDesign:Snackbar>
        </Grid>
    </materialDesign:DialogHost>
</Window>
