<UserControl x:Class="auth_elgamal.Views.Dialogs.ConfirmDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="165" d:DesignWidth="300"
             MaxWidth="400">
    <StackPanel Margin="16">

        <TextBlock Text="Підтвердження дії" 
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}" 
                   Margin="0,0,0,10"/>

        <TextBlock Text="Це призведе до затирання вашої поточної пари ключів. Цю дію неможливо скасувати. Ви впевнені, що хочете продовжити?"
                   TextWrapping="Wrap"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">

            <Button Content="Так, згенерувати"
                    Style="{DynamicResource MaterialDesignRaisedButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    CommandParameter="true"/>

            <Button Content="Ні, скасувати"
                    Style="{DynamicResource MaterialDesignFlatButton}"
                    Margin="8,0,0,0"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    CommandParameter="false"/>

        </StackPanel>
    </StackPanel>
</UserControl>