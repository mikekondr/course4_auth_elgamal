<UserControl x:Class="auth_elgamal.Views.DriveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Доступні диски" FontSize="18" FontWeight="Medium" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Disks}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Width="250" Margin="10" VerticalAlignment="Top">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{Binding DiskName}" 
                                       FontSize="16" FontWeight="Bold" 
                                       Margin="10"
                                       Foreground="{DynamicResource PrimaryHueMidBrush}"/>

                                <ListBox Grid.Row="1" Height="150" Margin="10"
                                         ItemsSource="{Binding Files}"
                                         Visibility="{Binding CanRead, Converter={StaticResource BoolToVisibility}}"
                                         SelectionMode="Single">

                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="Transparent" ToolTip="Натисніть, щоб прочитати файл">

                                                <StackPanel Orientation="Horizontal" Margin="8,4">
                                                    <materialDesign:PackIcon Kind="FileOutline" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding FileName}" VerticalAlignment="Center"/>
                                                </StackPanel>

                                                <Border.InputBindings>
                                                    <MouseBinding MouseAction="LeftClick" 
                                                                  Command="{Binding ReadFileCommand}"/>
                                                </Border.InputBindings>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>

                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                        </Style>
                                    </ListBox.ItemContainerStyle>

                                </ListBox>

                                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">

                                    <Button Content="Створити" 
                                        Style="{DynamicResource MaterialDesignTextButton}"
                                        IsEnabled="{Binding CanWrite}"
                                        Command="{Binding CreateFileCommand}"/>

                                    <Button Content="Запустити"
                                        Style="{DynamicResource MaterialDesignTextButton}"
                                        IsEnabled="{Binding CanExecute}"
                                        Command="{Binding ExecuteCommand}"
                                        Margin="5,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>