<UserControl x:Class="auth_elgamal.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Padding="10" 
                BorderThickness="0,0,0,1" 
                BorderBrush="{DynamicResource MaterialDesignDivider}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="{Binding WelcomeMessage}" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           VerticalAlignment="Center"/>

                <Button Grid.Column="1" 
                        Content="Вийти" 
                        Command="{Binding LogoutCommand}"
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <TabControl Grid.Row="1"
                    Margin="5"
                    SelectedIndex="{Binding SelectedTabIndex}">

            <TabItem>
                <ContentControl Content="{Binding DriveVM}"/>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Harddisk" Margin="0,0,5,0"/>
                        <TextBlock Text="Доступ до дисків"/>
                    </StackPanel>
                </TabItem.Header>
            </TabItem>

            <TabItem>
                <ContentControl Content="{Binding EncryptionVM}"/>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Lock" Margin="0,0,5,0"/>
                        <TextBlock Text="Шифрування"/>
                    </StackPanel>
                </TabItem.Header>
            </TabItem>

        </TabControl>

    </Grid>
</UserControl>