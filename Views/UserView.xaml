<UserControl x:Class="auth_elgamal.Views.UserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" 
    d:DesignHeight="300"
    d:DesignWidth="300">

    <StackPanel>
        <TextBlock Text="{Binding WelcomeMessage}" FontSize="20" Margin="10"/>
        <Button Content="Вийти" Width="100" Margin="10" Command="{Binding LogoutCommand}"/>
    </StackPanel>
</UserControl>